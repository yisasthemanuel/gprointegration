spring:
  application:
    name: yisas-gprointegration
  # CLOUD CONFIG
  config:
    import: "optional:configserver:"
#eureka:
#  client:
#    service-url:
#      defaultZone: ${EUREKA_URI:http://localhost:8761/eureka}
#  instance:
#    prefer-ip-address: true    